const apipath = import.meta.env.APIPATH || 'https://localhost:2024'

export default apipath 

//When deploying change the env apipath in api.js to the url of the hosted server(this url comes from whatever the hosting plkatform hostinger or whatever...)